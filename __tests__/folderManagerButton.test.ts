import { describe, expect, it } from 'vitest';
import FolderManagerButton from '@/components/FolderManagerButton';

describe('FolderManagerButton', () => {
  it('exports a component', () => {
    expect(typeof FolderManagerButton).toBe('function');
  });
});
